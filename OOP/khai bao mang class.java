// Khai báo mảng students với n phần tử
Student[] students = new Student[n];
for (int i = 0; i < n; i++) {
	// Khởi tạo các phần tử trong mảng students
	students[i] = new Student(); // Trong Java, khi bạn muốn tạo một đối tượng của một lớp, bạn cần sử dụng từ khóa new để gọi constructor của lớp đó.
}
